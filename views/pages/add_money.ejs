<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Boost Your Balance - Campus Cash</title>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Nunito', sans-serif;
            background-color: #f9f9f9;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background-color: white;
            border-radius: 20px;
            padding: 40px;
            width: 100%;
            max-width: 600px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            position: relative;
        }

        h1 {
            color: #ff6b6b;
            font-size: 32px;
            text-align: center;
            margin-bottom: 30px;
        }

        .input-group {
            margin-bottom: 25px;
        }

        label {
            display: block;
            margin-bottom: 10px;
            color: #4a4a4a;
            font-weight: 700;
            font-size: 18px;
        }

        input[type="number"] {
            width: 100%;
            padding: 15px;
            border: 2px solid #4ecdc4;
            border-radius: 10px;
            font-size: 20px;
            transition: border-color 0.3s;
        }

        input[type="number"]:focus {
            outline: none;
            border-color: #45b7aa;
        }

        .submit-btn {
            background-color: #4ecdc4;
            color: white;
            border: none;
            border-radius: 10px;
            padding: 18px;
            width: 100%;
            font-size: 20px;
            font-weight: 700;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .submit-btn:hover {
            background-color: #45b7aa;
        }

        .mascot {
            text-align: center;
            margin-bottom: 30px;
        }

        .mascot img {
            max-width: 150px;
        }

        .form-container {
            max-width: 500px;
            margin: 0 auto;
        }

        .back-btn {
            position: absolute;
            top: 20px;
            left: 20px;
            background-color: #ff6b6b;
            color: white;
            border: none;
            border-radius: 5px;
            padding: 10px 15px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .back-btn:hover {
            background-color: #ff5252;
        }
    </style>
</head>

<body>
    <div class="container">
        <button class="back-btn" onclick="goBack()">← Back</button>
        <div class="mascot">
            <img src="college_mascot.png" alt="College Mascot">
        </div>
        <h1>Boost Your Campus Cash!</h1>
        <div class="form-container">
            <form method="post" action="">
                <div class="input-group">
                    <label for="amount">How much would you like to add?</label>
                    <input type="number" id="amount" name="amount" placeholder="Enter amount" required min="0.01"
                        step="0.01">
                </div>
                <button type="submit" class="submit-btn">Load It Up!</button>
            </form>
        </div>
    </div>

    <script>
        function goBack() {
            window.history.back();
        }
    </script>
</body>

</html>